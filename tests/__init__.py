"""Tests package for linkhut-lib.

The package layout (with `__init__.py`) lets pytest import tests as
`tests.test_*` and use relative imports like `from ._helpers import
...`. The empty body is intentional — pytest does the discovery work
and the individual test modules own their imports.
"""
